/** @type {import('tailwindcss').Config} */
export default {
  // scan only your source files — don't reference optional node_modules packages
  content: ['./src/**/*.{html,js,svelte,ts}'],
  theme: {
    extend: {},
  },
  plugins: [
  ],
}
